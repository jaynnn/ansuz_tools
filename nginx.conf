# Ansuz Tools Nginx 配置模板
# 将此文件复制到 /etc/nginx/sites-available/ansuz_tools
# 然后创建符号链接: ln -s /etc/nginx/sites-available/ansuz_tools /etc/nginx/sites-enabled/
# 修改 server_name 为您的域名，并配置 SSL 证书路径

# HTTP -> HTTPS 重定向
server {
    listen 80;
    server_name yourdomain.com;

    # 允许上传较大的音频文件（智谱音频分析功能需要）
    client_max_body_size 20m;

    location / {
        proxy_pass http://localhost:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS 配置（使用 Let's Encrypt）
# server {
#     listen 443 ssl;
#     server_name yourdomain.com;
#
#     # 允许上传较大的音频文件（智谱音频分析功能需要）
#     # 注意：此设置必须在 HTTPS server 块中也配置，否则音频上传将报 413 错误
#     client_max_body_size 20m;
#
#     ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://localhost:4000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
